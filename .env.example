# Variáveis de ambiente para o Vercel
# Copie este arquivo para .env.local no desenvolvimento

# Configurações do Vercel KV (opcional, para rate limiting)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Outras variáveis de ambiente (se necessário)
# NODE_ENV=production